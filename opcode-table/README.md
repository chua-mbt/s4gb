# Opcode Table Generator

Generates a static HTML opcode table for the Game Boy CPU instruction set.

## Usage

```bash
sbt "opcodeTable/run"
```

Output is written to `opcode-table/target/docs/opcode-table.html`.
